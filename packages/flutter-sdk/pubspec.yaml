name: clawface_flutter_sdk
description: Flutter SDK for ClawFace — the missing UI layer for AI agents. Schema-driven CRUD client with Provider integration.
version: 0.1.0
homepage: https://github.com/AiClawFace/clawface

environment:
  sdk: ^3.0.0
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
